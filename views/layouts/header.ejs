<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Les Audacieuses - <%= title %></title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="dashboard-layout">
    <header class="main-header">        <div class="header-left">
            <div class="welcome-message">
                <% if (locals.user) { %>
                    <span>Bonjour <%= locals.user.firstname %></span>
                <% } else { %>
                    <span>Bonjour</span>
                <% } %>
            </div>
        </div>
        <div class="header-center">
            <div class="logo">
                <img src="/images/logo.svg" alt="Les Audacieuses Logo">
            </div>
        </div>
        <div class="header-right">
            <nav class="header-nav">
                <a href="/dashboard" class="<%= typeof active !== 'undefined' && active === 'dashboard' ? 'active' : '' %>">
                    <img src="/images/home.svg" alt="Accueil">
                </a>
                <a href="/modules" class="<%= typeof active !== 'undefined' && active === 'modules' ? 'active' : '' %>">
                    <img src="/images/book.svg" alt="Modules">
                </a>
                <a href="/patients" class="<%= typeof active !== 'undefined' && active === 'patients' ? 'active' : '' %>">
                    <img src="/images/person.svg" alt="Patientes">
                </a>
                <a href="/logout" class="logout-btn">
                    <span>Déconnexion</span>
                </a>
            </nav>
        </div>
    </header>

    <div class="content-wrapper">
        <main class="main-content">